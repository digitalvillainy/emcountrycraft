# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Stripe

# Model Form Field configuration
form: $/rbm/stripe/models/stripe/fields.yaml

# Model Class name
modelClass: Rbm\Stripe\Models\Stripe

# Default redirect location
defaultRedirect: rbm/stripe/stripe

# Create page
create:
    title: backend::lang.form.create_title
    redirect: rbm/stripe/stripe/update/:id
    redirectClose: rbm/stripe/stripe

# Update page
update:
    title: backend::lang.form.update_title
    redirect: rbm/stripe/stripe
    redirectClose: rbm/stripe/stripe

# Preview page
preview:
    title: backend::lang.form.preview_title
